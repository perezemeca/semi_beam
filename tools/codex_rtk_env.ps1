# Script manual opcional para sesiones Codex/RTK en PowerShell.
# La preparación inline preferida es:
# $env:Path = "$PWD\.venv\Scripts;$env:Path"

$env:Path = "$PWD\.venv\Scripts;$env:Path"
Write-Host "PATH preparado para .venv local."
